**To create project:**
Go to the project (empty) folder
```bash
pnpm create next-app .
```

**To access the project URL**
```bash
pnpm dev
```

**For icon**

https://react-icons.github.io/react-icons/
```bash
pnpm install react-icons --save
```

**Use workos.com for authentication (user login)**

https://workos.com/
```bash
pnpm add @workos-inc/node
pnpm add @workos-inc/authkit-nextjs
```

**Radix Themes is a pre-styled component library that is designed to work out of the box with minimal configuration**

https://www.radix-ui.com/
```bash
pnpm add @radix-ui/themes
```

**To create a responsive country, state, city dropdown search options**

https://yarnpkg.com/package?q=city&name=country-state-city
```bash
pnpm add react-country-state-city
```

**Use AWS S3 to store company logos**
- First we need to create a S3 bucket, a IAM user, and add S3 permission into this IAM user. Specify this user to this S3 bucket.
- Then inside this user, we create an **access key** and add to our `.env` file
```bash
pnpm add axios
pnpm add @aws-sdk/client-s3
```

**Use MongoDB as database to save created jobs**

https://www.mongodb.com/

https://cloud.mongodb.com/v2/60f73f15da9cca02a90ca126#/clusters

```bash
pnpm add mongoose
```

**Timestamp for time-ago**

```bash
pnpm add react-timeago
```